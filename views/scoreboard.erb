<div class="container">
  <div class="row">
    <div class="twelve columns" style="margin-top: 10%">
     <div style="text-align:center;margin-bottom:50px;">
        <h2>Game Over!</h2>
      </div>

      <div class="u-pull-left">
        <h1>Top Players</h1>
      </div>
      <div class="u-pull-right">
        <a class="button button-primary" href="/">Play Again</a>
      </div>
      <br class="u-cf" />
      <% @grouped_games.each do |genre,games| %>
	<div class="scorecard">
          <h5><%=settings.genres[genre]%></h5>
          <table class="u-full-width">
            <thead>
              <tr>
		<th>#</th>
                <th>Name</th>
                <th>Score</th>
                <th>Date Played</th>
              </tr>
            </thead>
            <tbody>
              <% games.each_with_index do |game,index| %>
                <tr class="<%=params[:last_game_id]==game.id.to_s ? 'last_game' : ''%>">
		  <td><%=index + 1%></td>
                  <td><%=game[:username]%></td>
                  <td style="text-align:center;"><strong><%=game[:score]%></strong></td>
                  <td><%=game[:created_at].strftime("%m/%d/%Y")%></td>
                </tr>
              <% end %>
            </tbody>
          </table>
	</div>
      <% end %>
    </div>
  </div>
</div>

